LuckyNumber V6.4.7

Update scope:
- Faster/smoother bottom navigation on iPhone/PWA.
- New supplied 365 + star Home Screen icon.
- Duplicate AI action handlers removed.
- No formula, history schema, backup schema, or stored-data migration changed.

Deploy all files together to GitHub Pages so the new service-worker cache and icons update correctly.
If iPhone still shows the previous icon, remove the old Home Screen shortcut once and Add to Home Screen again after the site is updated; iOS can cache Home Screen icons separately.
