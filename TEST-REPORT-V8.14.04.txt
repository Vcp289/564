LuckyNumber V8.14.04 — Rank Score Warmup / Daily Update / iOS / Auto Route Pro

Fix:
- Analysis Profile AI Ranking no longer hides Rank Score until 8 trusted rows.
- A profile with >=1 trusted scored row now displays a Bayesian-shrunk Rank Score immediately.
- `evidenceReady` (>=8 trusted rows) is retained as the mature-evidence sort guard, so warmup profiles cannot jump above mature profiles merely from a tiny sample.
- Profiles with 0 trusted scored rows still display “—”.
- Updated / Waiting result badge logic is unchanged.
- AUTO Route / History / WF / persistence engines are unchanged except versioned asset filenames.

Regression checks:
- 1 trusted row => scoreReady=true and numeric Rank Score.
- 3 trusted rows => numeric Rank Score.
- 7 trusted rows => numeric Rank Score, still Warmup.
- 8+ trusted rows => numeric Rank Score and mature evidence.
- 0 trusted rows => dash.
- Sorting still prioritizes mature evidence before warmup evidence.
- JavaScript syntax check: PASS.
- Version / Service Worker / asset references: PASS.
