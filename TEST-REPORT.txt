LuckyNumber V7.22.11 — HISTORY STORE RESET PRO
Base: V7.22.10-V706-SAVE-FLOW-AUTO-V2-PRO-TESTED

Architecture reset scope
- Actual Result is now the single source of truth for History.
- Canonical identity: profileId + drawDate.
- Manual Save / Edit / Image Import use the same upsert rule.
- Duplicate same-day rows are not appended anymore; they update/merge the canonical row.
- Existing legacy duplicate rows are normalized on load; losing row IDs receive delete tombstones so recovery cannot resurrect them.
- Draw Date stays independent from reference/table date.
- Delete removes source row + source-linked daily table + source-linked record through one source-store function.
- Foreground persistence uses the compact History source transaction first; full-state/IndexedDB work remains recovery/background.
- Derived AI/X3/P18/P19/WF algorithms were not redesigned in this patch.

Tests
PASS: node --check app-v72211.js
PASS: node --check sw-v72211.js
PASS: node --check auto-route-v2-v72211.js
PASS: source-store insert
PASS: same Profile+date second write keeps exactly one row
PASS: overwrite preserves stable row ID
PASS: legacy duplicate canonicalization
PASS: duplicate tombstone generation
PASS: delete removes source-linked dailyTable and History record
PASS: package references updated to v72211

Expected regression target
- Saving Hanoi normal 26 Aug must create/update key profileId|2026-08-26 only.
- Reference table may still be 25 Aug without changing the History draw date.
- History must no longer say 26 Aug already exists while showing only a hidden duplicate path caused by append semantics.
