LuckyNumber V7.20.86t • DURABLE DELETE TRANSACTION PRO
Base: V7.20.86s Analysis Cold-Boot Authority Pro
Date: 2026-08-27

ROOT CAUSE
- History UI called deleteActualDrawWithSync() from inline delete and detail delete,
  but the function was missing from the codebase, causing a runtime ReferenceError.

PRO FIX
1) New durable delete transaction
   - Delete Actual row by immutable ID.
   - Remove derived records linked by actualDrawId.
   - Remove auto-generated daily table linked by sourceActualDrawId.
   - Clear dangling manual referenceTableId links when their source table disappears.

2) Cold-kill resurrection guard
   - New _actualDrawDeleteJournal tombstones are persisted in MAIN and History checkpoints.
   - mergeRecoveredHistory filters stale recovery rows/tables/records through tombstones.
   - Old IndexedDB/localStorage recovery snapshots cannot resurrect an explicitly deleted row.

3) Transaction safety
   - Snapshot narrow mutable collections before delete.
   - MAIN durable commit is required before success UI.
   - Rollback in-memory mutation if primary durability fails.
   - Synchronous History source checkpoint closes the iOS suspend window.

4) Targeted derived invalidation
   - Latest-row delete uses the existing fast WF prefix prune when safe.
   - Older-row delete invalidates only that Profile WF generation.
   - AI History / L links refresh in background; no global Full Rebuild is forced.

TESTS
PASS: deleteActualDrawWithSync is defined and both UI call sites resolve
PASS: node --check app-v72086t.js
PASS: node --check pro-core-v72086t.js
PASS: node --check x3-pro-v72086t.js
PASS: node --check sw-v72086t.js
PASS: manifest-v72086t.json parse
PASS: delete tombstone included in sync/full History checkpoint
PASS: stale recovery merge filters deleted Actual/record/table IDs
PASS: PWA build token 72086tdurabledelete consistent
PASS: ZIP integrity
