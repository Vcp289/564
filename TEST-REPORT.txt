LuckyNumber V7.22.17 • Analysis SWR Self-Heal Pro
TESTED 2026-08-29

Fixes verified in source/runtime gates:
1. Analysis Recent Winner uses Stale-While-Revalidate (exact snapshot -> last committed fallback).
2. Missing exact snapshots automatically enqueue profile-scoped self-heal after first paint.
3. Analysis self-heal queue is deduplicated and sequential (prevents 19 concurrent heavy jobs on iPhone).
4. Successful repair atomically republishes History summary + ranking and refreshes Analysis.
5. Ranking text now says "profiles มีผลวันที่ล่าสุด"; it is NOT shown as a sync-progress counter.
6. Profiles without a result on global latest draw display "Waiting result", not ambiguous "Pending" sync status.
7. History/Analysis foreground navigation remains snapshot-first and does not block on Rebuild.
8. No manual Rebuild is required as a normal prerequisite; Rebuild remains recovery/maintenance only.

Automated gates passed:
- node --check: app, service worker, pro-core, auto-route, X3
- JSON parse: manifest + version
- asset-reference existence check
- build/version/service-worker consistency check
- source assertions for SWR fallback + Analysis self-heal + label semantics
- ZIP integrity test
