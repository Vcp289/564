LuckyNumber V7.20.92 — STRICT REBUILD CONTROL PRO
Date: 2026-08-28

GOAL
- Historical edit/delete never rebuilds from Profile day 1 when a trusted WF prefix exists.
- Latest delete prunes one WF row.
- History / Analysis / AI navigation cannot start missing-WF rebuilds.
- Check Update / Refresh cannot resume a stale global job when all current WF buckets already prove healthy.
- Global Clean Rebuild creators remain restricted to incompatible JSON Restore and explicit Manual Rebuild.

CHANGES VERIFIED
1) Historical Save/Edit
   rebuildWalkForwardBacktest(profileId, startDate=affectedDate, mutationScope=true)
   => verified prefix before affectedDate is reused; affected suffix only is recalculated.

2) Historical Delete
   rebuildWalkForwardBacktest(profileId, startDate=deletedDate, mutationScope=true)
   => verified prefix before deletedDate is reused; affected suffix only is recalculated.

3) Latest Delete
   fastPruneLatestWalkForwardAfterDelete(...)
   => latest WF row is removed without historical rebuild when dependency-free.

4) Read-only navigation
   renderHistory() no longer calls ensureProfileDerivedHistoryReady().
   renderAnalysisFresh() no longer calls ensureProfileDerivedHistoryReady().
   AI page/cold-boot hydration uses scheduleMissing:false.

5) Update / PWA
   PWA_SW_URL = sw-v72092.js (exact current build).
   Deferred startup reconciles a stale pending job to Rebuild 0 when every current WF bucket passes current History + engine + strict prior-only verification.

6) Full Global Rebuild authority
   createWalkForwardRebuildJob({cleanRebuild:true}) appears only in:
   - JSON Restore cache proof failure / incompatible derived cache path.
   - fullSystemAiRebuild() explicit user action.

REGRESSION / INTEGRITY
- node --check app-v72092.js: PASS
- node --check pro-core-v72092.js: PASS
- node --check sw-v72092.js: PASS
- node --check x3-pro-v72092.js: PASS
- HTML versioned asset references: PASS
- X3 engine SHA-256 unchanged from V7.20.91:
  6716c5e9409889f2174c3700fa7ea8a2489b78c5fa8a1a41df69e0cfd9389d42
- pro-core unchanged from V7.20.91: PASS
- CSS unchanged from V7.20.91: PASS

EXPECTED REBUILD RULES
- Open app / switch page / History / Analysis / AI: 0 rebuild.
- Check Update / Refresh with compatible healthy cache: Reuse cache, Rebuild 0.
- Add newest draw: one-row resume where prior WF prefix exists.
- Edit newest draw: latest affected suffix (normally one row).
- Edit historical draw: one Profile, from affected date forward only.
- Delete newest draw: prune one row.
- Delete historical draw: one Profile, from deleted date forward only.
- Manual Rebuild: full Profile set with History.
- Incompatible JSON Restore: clean rebuild only where canonical restore path requires it.
