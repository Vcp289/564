LuckyNumber V7.20.86p • AI PAGE REWRITE PRO
Base: V7.20.86o AI Complete Pro
Date: 2026-08-27

GOAL
- Replace the unstable AI page flow with a fresh implementation instead of stacking more patches.
- Eliminate the hanging X3 Candidate Pick state.

REWRITE SCOPE
1) AI page composition rewritten
   - renderWeeklyFresh now paints a rebuilt AI page flow.
   - The page no longer depends on the old ai-pick runtime card.

2) AI Pick rebuilt from scratch
   - Source priority: AI Decision -> Profile AI Ranking fallback.
   - Candidate generation is quick and synchronous from existing X3 pool logic.
   - No historical rescoring loop, no endless "กำลังจัดอันดับ" state.
   - If no X3 pool exists, show a terminal no-data message instead of hanging.

3) Best Profiles preserved
   - Existing Profile Trend card and fallback behavior stay intact.

4) Old AI Pick detached
   - index no longer loads ai-pick-v72086p.js.
   - Rebuilt card uses a new root class so legacy refresh logic cannot overwrite it.

TESTS
PASS: node --check app-v72086p.js
PASS: node --check ai-pick-v72086p.js
PASS: node --check pro-core-v72086p.js
PASS: node --check x3-pro-v72086p.js
PASS: node --check sw-v72086p.js
PASS: manifest-v72086p.json parse
PASS: index detached old ai-pick runtime
PASS: rebuilt AI page renders with new quick-pick flow
PASS: ZIP integrity
