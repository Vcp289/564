LuckyNumber V7.22.16 • History Self-Heal Pro Final

ROOT CAUSE FIXED
- V7.22.15 History summary retry restored caches with scheduleMissing=false.
- When CLS / AIL / GL / P18 were genuinely absent, the retry only waited/retried.
- Therefore History could remain "—" indefinitely until the user pressed Rebuild.

V7.22.16 FIX
- Added one-at-a-time profile-scoped History derived self-heal queue.
- History and Analysis remain snapshot-first and open immediately.
- If the exact committed generation is missing/incomplete, self-heal starts in background.
- Self-heal reuses runAIHistoryTransaction serialization and targeted WF recovery only for that profile.
- Pending History summary now actively schedules repair instead of waiting forever.
- Successful repair atomically commits History rows + AI percentages + ranking and refreshes the visible page.
- Manual Rebuild is not part of normal History/Analysis startup.

STATIC TESTS
- node --check app-v72216.js: PASS
- node --check pro-core-v72216.js: PASS
- node --check x3-pro-v72216.js: PASS
- node --check auto-route-v2-v72216.js: PASS
- node --check sw-v72216.js: PASS
- stale v72215 asset references: none
- ZIP integrity: verified after packaging
